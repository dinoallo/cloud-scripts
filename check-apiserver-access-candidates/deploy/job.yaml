apiVersion: batch/v1
kind: Job
metadata:
  name: check-apiserver-access-candidates
  namespace: check-apiserver-access-candidates
  labels:
    app.kubernetes.io/name: check-apiserver-access-candidates
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app.kubernetes.io/name: check-apiserver-access-candidates
    spec:
      serviceAccountName: check-apiserver-access-candidates
      restartPolicy: Never
      containers:
        - name: scanner
          image: ghcr.io/dinoallo/check-apiserver-access-candidates:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: 20m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65532
            seccompProfile:
              type: RuntimeDefault
