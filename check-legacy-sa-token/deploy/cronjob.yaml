apiVersion: batch/v1
kind: CronJob
metadata:
  name: check-legacy-sa-token
  namespace: check-legacy-sa-token
  labels:
    app.kubernetes.io/name: check-legacy-sa-token
spec:
  schedule: "0 2 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 0
      ttlSecondsAfterFinished: 86400
      template:
        metadata:
          labels:
            app.kubernetes.io/name: check-legacy-sa-token
        spec:
          serviceAccountName: check-legacy-sa-token
          restartPolicy: Never
          containers:
            - name: scanner
              image: ghcr.io/dinoallo/check-legacy-sa-token:latest
              imagePullPolicy: IfNotPresent
              resources:
                requests:
                  cpu: 20m
                  memory: 64Mi
                limits:
                  cpu: 500m
                  memory: 256Mi
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: ["ALL"]
                readOnlyRootFilesystem: true
                runAsNonRoot: true
                runAsUser: 65532
                seccompProfile:
                  type: RuntimeDefault
